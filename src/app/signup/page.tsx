import {Header} from "@/components/Header/Header";
import React from "react";
import {Container} from "@mantine/core";

export default function Home() {
    return (
        <main>
            <Header />
            <Container size={1440} >
                <div>Work in process Sign up page</div>
            </Container>
        </main>
    );
}
